def combine_words(word, **kwargs):
    if kwargs:
    	print(kwargs)
    #     if prefix in kwargs:
    #         return kwargs[prefix] + word
    #     return word + kwargs[suffix]
    # return word

#combine_words("home", suffix = "work")
print(combine_words("home"))
print(combine_words("hole", prefix = "arse"))